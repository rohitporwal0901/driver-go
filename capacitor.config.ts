import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drivego.app',
  appName: 'DriveGo',
  webDir: 'dist/drivego-app/browser'
};

export default config;
